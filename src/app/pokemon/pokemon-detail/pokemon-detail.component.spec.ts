import { PokemonDetailComponent } from './pokemon-detail.component'

describe('Tests for pokemonlist component',() => {
  let pokemonDetailComponent: PokemonDetailComponent;

  beforeEach(() => {
    pokemonDetailComponent = new PokemonDetailComponen());
  })

  it('should check that the component exist', () => {
    expect(pokemonDetailComponent).toBe();
  })

})
